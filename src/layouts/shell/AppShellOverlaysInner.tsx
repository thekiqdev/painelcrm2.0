import GlobalSearchOverlay from './overlays/GlobalSearchOverlay';

/** Conteúdo pesado dos overlays (lazy via AppShellOverlays). */
export default function AppShellOverlaysInner() {
  return <GlobalSearchOverlay />;
}
