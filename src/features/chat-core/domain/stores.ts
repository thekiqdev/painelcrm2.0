/**
 * Placeholders de stores internos do Chat Core (F0).
 * Implementação F5.0 em `store/` — não wired à UI até F5.1+.
 */

export {
  conversationStorePlaceholder,
  messageStorePlaceholder,
  instanceRegistryPlaceholder,
  unreadEnginePlaceholder,
} from '../core/chatCore';
