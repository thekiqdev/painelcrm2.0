/**
 * Billing Engine V2 — conversão snapshot → invoice item (Sprint futura).
 */
import type { BillingItemSnapshot } from './types.js';

export function mapBillingItemSnapshotToInvoiceItem(_snapshot: BillingItemSnapshot): never {
  throw new Error('not_implemented');
}
